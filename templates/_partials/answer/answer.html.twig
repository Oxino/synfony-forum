<div class="card mb-3">
	<div class="card-header">{{ answer.author.username }}</div>
	<div class="card-body">
		<blockquote class="blockquote mb-0">
			<p>{{ answer.content }}</p>
			<footer class="blockquote-footer mt-3">{{ answer.publishedDate|date('d/m/Y H:i') }}</footer>
		</blockquote>
	</div>
</div>
